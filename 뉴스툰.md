# 뉴스툰 (NEWS TOON) 개발 기록

## 개요
AI가 부동산 뉴스를 분석하여 해설 기사 + 웹툰 이미지를 자동 생성하는 시스템

---

## 아키텍처

### 3단계 파이프라인
```
뉴스 입력 → [1] 해설 기사 생성 (Gemini Text) → [2] 웹툰 스크립트 생성 (Gemini Text) → [3] 웹툰 이미지 생성 (Gemini Pro Image)
```

### 기술 스택
- **텍스트 생성**: `gemini-2.5-flash` (해설 기사 + 웹툰 스크립트)
- **이미지 생성**: `gemini-3-pro-image-preview` (웹툰 이미지) ← Flash에서 업그레이드
- **패키지**: `@google/genai` v1.42.0
- **저장소**: Supabase Storage (`ai-photos` 버킷 > `toon-images/` 폴더)
- **DB**: Supabase `news_toon_episodes` 테이블

### 모델 비교
| 모델 | 속도 | 이미지 크기 | 한글 품질 | 비고 |
|------|------|------------|-----------|------|
| `gemini-2.5-flash-image` | ~15초 | ~1.4MB | ❌ 한글 오타 많음 | 폐기 |
| `gemini-3-pro-image-preview` | ~30초 | ~700KB~900KB | ✅ 한글 거의 정확 | **현재 사용** |

---

## 캐릭터 설정 (동물 캐릭터)

| 키 | 이름 | 역할 | 이미지 설명 |
|----|------|------|------------|
| cat | 고양이 기자 | 부동산 뉴스 진행자 | 안경 쓴 하얀 고양이 + 마이크 + 기자조끼 |
| dog | 영끌남 | 실수요자/일반 시민 | 서류 든 베이지 강아지, 걱정 표정 |
| fox | 여우 관료 | 정부 관료/정책 발표자 | 검은 정장 오렌지 여우, 빨간 넥타이 |
| horse | 말 소장 | 부동산 소장/중개사 | 셔츠+넥타이 갈색 말, 친근한 전문가 |
| squirrel | 다람쥐 아내 | 잔소리 마누라/현실주의 | 노란 후드 다람쥐 + 열쇠 |
| owl | 올빼미 교수 | 전문가/분석가 | 안경+클립보드 올빼미 |

- 폴백 이미지: `public/images/toon/characters/` (cat.png, dog.png 등)

---

## 주요 파일

### 핵심 로직
- `src/lib/ai-toon.ts` — 3단계 파이프라인 (기사 생성 → 스크립트 생성 → 이미지 생성)
  - `generateToonFromNews()` — 메인 함수
  - `buildImagePrompt()` — 패널 스크립트 → 이미지 프롬프트 변환
  - `generateToonImage()` — Gemini Pro로 이미지 생성 + Supabase 업로드
  - `generateSlug()` — 한글 지원 URL slug 생성

### API 라우트
- `src/app/api/toon/route.ts` — GET: 발행된 에피소드 목록 (status=published 필터)
- `src/app/api/toon/generate/route.ts` — POST: AI 뉴스툰 생성 (관리자 전용)
- `src/app/api/toon/[slug]/route.ts` — GET: 에피소드 상세 + 조회수 증가

### 프론트엔드
- `src/app/toon/page.tsx` — 에피소드 목록 페이지 (다크 테마, 그리드 카드)
- `src/app/toon/[slug]/page.tsx` — 에피소드 상세 (2컬럼: 좌=기사, 우=웹툰 이미지)

---

## DB 스키마: `news_toon_episodes`

| 컬럼 | 타입 | 설명 |
|------|------|------|
| id | UUID | PK |
| episode_number | INTEGER | 에피소드 번호 |
| title | TEXT | AI 생성 제목 |
| subtitle | TEXT | AI 생성 부제 |
| slug | TEXT | URL slug (한글 지원) |
| category | TEXT | 시장동향/분양정보/정책/전망/부동산 |
| article_html | TEXT | AI 해설 기사 (HTML) |
| article_summary | TEXT | SNS 공유용 한줄 요약 |
| panels | JSONB | 웹툰 패널 스크립트 (4~8컷) |
| toon_image_url | TEXT | AI 생성 웹툰 이미지 URL |
| thumbnail_url | TEXT | 썸네일 (미사용) |
| source_news_url | TEXT | 원문 뉴스 URL |
| source_news_title | TEXT | 원문 뉴스 제목 |
| status | TEXT | draft/published |
| published_at | TIMESTAMPTZ | 발행일 |
| view_count | INTEGER | 조회수 |
| created_at | TIMESTAMPTZ | 생성일 |

---

## 패널 데이터 구조 (새 형식)

```typescript
interface ToonPanel {
  panel: number;           // 패널 번호
  characters: string[];    // 등장 캐릭터 배열
  dialogue: Record<string, string>;  // 캐릭터별 대사
  scene: string;           // 장면 묘사 (이미지 프롬프트에 사용)
  mood: string;            // happy|angry|sad|shocked|smug|neutral
  props: string[] | null;  // 소품 목록
  text_overlay: string | null;  // 패널 위 텍스트 ("3년 후" 등)
  sfx: string | null;      // 효과음
}
```

---

## 이미지 프롬프트 구조

이미지 프롬프트는 `buildImagePrompt()`에서 자동 조립:
- 패널 수에 따라 2xN 그리드 레이아웃
- 제목 배너 (다크 네이비 배경)
- 스타일: 치비 한국 웹툰, 파스텔 색상, 두꺼운 선, 한글 말풍선
- 캐릭터 일관성 강조
- 각 패널: 캐릭터 영어 설명 + mood + 한글 대사 + 장면 + 소품 + SFX

---

## 생성된 에피소드

### EP.001 — 규제로 집값 잡겠다? 시장은 칼 피하는 법부터 배운다
- **카테고리**: 정책
- **패널**: 4컷 (여우 관료, 영끌남, 말 소장)
- **이미지**: Pro 모델로 재생성 완료 (2026-02-25)
- **기사**: 중복 타임라인 제거 + 전문가 톤으로 수정

### EP.002 — 이 대통령, '산골 밭값 30만원' 경고! 농지 투기 칼 빼든다
- **카테고리**: 정책
- **패널**: 6컷 (고양이 기자, 영끌남, 여우 관료, 말 소장, 올빼미 교수, 다람쥐 아내)
- **이미지**: Pro 모델로 생성 (916KB)
- **원문**: 한겨레 2026-02-24

---

## 스크립트 (scripts/)

| 파일 | 용도 |
|------|------|
| `generate-farmland-toon.js` | 뉴스 → 웹툰 생성 (독립 실행, dotenv 포함) |
| `upgrade-ep001.js` | EP.001 기사 수정 + Pro 이미지 재생성 |
| `test-gemini-image.js` | Gemini 이미지 모델 테스트 |
| `upload-test-toon-image.js` | 테스트 이미지 업로드 |
| `add-toon-image-column.js` | toon_image_url 컬럼 추가 |

---

## 해결된 이슈

### 한글 오타 문제
- **원인**: `gemini-2.5-flash-image` 모델의 한글 렌더링 품질 낮음
- **해결**: `gemini-3-pro-image-preview`로 업그레이드 → 한글 거의 정확

### JSON 파싱 오류
- **원인**: Gemini 응답의 article_html에 줄바꿈이 포함되어 JSON 파싱 실패
- **해결**: `extractJSON()` 함수 개선 + 줄바꿈 이스케이프 폴백 추가

### 모델 404 에러
- **시도**: `gemini-2.5-flash-preview-05-20` → 404
- **시도**: `imagen-3.0-generate-002` → 404
- **해결**: `gemini-2.5-flash-image` 사용 → 이후 `gemini-3-pro-image-preview`로 최종 업그레이드

---

## 향후 계획
- [ ] 관리자 대시보드에서 뉴스 URL 입력 → 원클릭 생성
- [ ] 에피소드 목록에 toon_image_url 썸네일 표시
- [ ] 자동 뉴스 크롤링 → 일일 자동 생성
- [ ] 이미지 생성 실패 시 재시도 로직
- [ ] 에피소드 수정/삭제 관리 기능

---

*Last Updated: 2026-02-25*
