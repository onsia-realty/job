# 부동산인(BOOIN) 회원가입 매뉴얼

> 실제 가입 화면(`/agent/auth/signup`) 기준. 작성 2026-06-15.
> 용도: ① 신규 유저 안내 ② 직접 테스트할 때 체크리스트.

---

## 1. 들어가는 길 (진입점)

- 로그인 페이지 `/agent/auth/login` → 하단 **"회원가입"** 링크 → `/agent/auth/signup`
- 가입 방식 2가지:
  - **이메일 가입** — 이메일+비밀번호 직접 입력 (➜ 이메일 인증 필요)
  - **소셜 로그인** — 카카오/구글 (➜ 인증메일 없이 추가정보만 입력)

---

## 2. 회원 유형 선택 (맨 위 탭)

| 탭 | 누구 | 가입 후 이동 |
|----|------|--------------|
| **개인회원** | 일자리 찾는 사람(구직자) | 채용공고 둘러보기 `/agent/jobs` |
| **기업회원** | 인재 찾는 사람(구인) | 구인 시작하기 `/agent/employer` |

> 기업회원을 골라야 **중개사무소 정보 입력란**이 나온다. (구인글 올리려면 기업회원)

---

## 3. 입력 항목 (이메일 가입 기준)

| 항목 | 필수 | 규칙 | 비고 |
|------|:---:|------|------|
| **이메일** | ✅ | 형식 검사 + **[중복확인] 버튼 눌러야 가입 가능** | 누르면 "사용 가능한 이메일입니다" 떠야 함 |
| **비밀번호** | ✅ | **8자 이상 + 영문·숫자 포함** | 👁 버튼으로 표시/숨김 |
| **비밀번호 확인** | ✅ | 위와 동일하게 | |
| **이름 / 이름(기업명)** | ✅ | — | 기업회원이면 라벨이 "이름(기업명)"으로 바뀜 |
| **닉네임** | ✅ | **2~12자, 한글·영문·숫자·`_`만** | 공인중개사 상호명 용도 |
| **연락처** | ✅ | `010-0000-0000`, 입력 벗어나면 자동 중복확인 | 중복이면 "이미 등록된 연락처" |
| **사업자번호** | ⬜ 선택 | `000-00-00000` 자동 포맷 | 나중에 마이페이지에서도 등록 가능 |
| **중개사무소 정보** | ⬜ 선택 | 기업회원만 노출. 아래 4번 참고 | 구인글 작성 시 인증 필요 |
| **약관 동의** | ✅ | **[필수] 이용약관 + 개인정보** 2개 체크 | 마케팅은 선택 |

> 소셜 가입이면: 이메일은 **소셜 계정 이메일로 고정(수정 불가)**, 비밀번호 입력란 없음. 나머지(이름·닉네임·연락처·약관)는 동일하게 입력.

---

## 4. 기업회원 — 중개사무소 정보 (선택)

- **개설등록번호** 입력(예: `11710-2022-00250`) → **[조회]** 버튼 → 공공데이터로 **중개사무소명·소재지·개설일·영업상태 자동 조회**.
- 조회 성공하면 초록 박스로 "확인된 중개사무소" 표시.
- ⚠️ **지금 안 넣어도 가입은 된다.** 단, **구인글 작성할 때** 중개사무소 또는 사업자번호 인증이 필요함.

---

## 5. 가입 완료 후

- **이메일 가입** → "이메일 인증이 필요합니다" 화면 → **받은 메일의 인증 링크 클릭해야 최종 완료.** (스팸함 확인)
- **소셜 가입** → 바로 "프로필 등록 완료!" → 서비스 이용 가능.

---

## 6. 🧪 직접 테스트할 때 (꿀팁 — 테스트가 어려운 이유 해결)

이메일 가입은 **인증메일**을 받아야 해서 번거롭다. 아래 중 편한 걸로:

### 방법 A — Gmail "+ 별칭" (가장 쉬움, 추천)
- `realtors77@gmail.com` 쓴다면 → `realtors77+test1@gmail.com`, `+test2`... 로 **무한정 테스트 계정** 생성 가능.
- 인증메일은 전부 **원래 받은편지함(realtors77)** 으로 옴. 링크 클릭하면 완료.
- 같은 받은편지함이라 확인 편함.

### 방법 B — 소셜 로그인으로 테스트 (인증메일 없음)
- 카카오/구글로 가입 → 인증메일 단계 자체가 없어 **제일 빠름**.

### 방법 C — Supabase에서 수동 인증 처리
- 인증메일 못 받는 상황이면: Supabase 대시보드 → **Authentication → Users** → 해당 유저 → **이메일 confirm 처리**.

### 테스트용 정상 사업자번호
- 입력검증 통과되는 실제 유효번호: **`124-81-00998`** (체크섬 정상, 법인).
- 개설등록번호는 실제 번호가 있어야 조회됨 → 없으면 **그냥 비우고 가입**(선택사항).

### 테스트 후 정리 (중요)
- 테스트 계정은 **Supabase → Authentication → Users**에서 삭제.
- 함께 `users` 테이블 행, 테스트로 올린 공고도 삭제하면 깔끔.
- (테스트 계정은 `+test`, 닉네임 `QA` 등으로 알아보기 쉽게 만들 것)

---

## 7. ✅ "가입 잘 됐다" 확인 포인트

1. 가입 완료 화면이 뜬다 (이메일이면 인증 안내, 소셜이면 완료).
2. Supabase **`users` 테이블**에 행이 생겼다 (email, name, nickname, user_type 채워짐).
3. 기업회원이면 `user_type = employer`, 중개사무소 입력했으면 `broker_*` 채워짐.
4. 로그인 후 기업회원은 `/agent/employer`, 개인회원은 `/agent/jobs`로 들어가진다.

> 이 4개가 다 맞으면 가입 동선 정상. (2026-06-15 자동 점검에서 전 단계 통과 확인됨)
